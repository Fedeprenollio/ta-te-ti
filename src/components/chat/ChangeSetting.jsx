import React from 'react'

export const ChangeSetting = ({socket}) => {
useEffect(() => {
    if(!socket)return

    
  return () => {
    
  }
}, [socket])



  return (
    <div>ChangeSetting</div>
  )
}
